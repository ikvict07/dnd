<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="02" author="liquibase">
        <comment>Create enums for DnD Combat System</comment>
        
        <!-- attribute_type -->
        <sql>create type attribute_type as enum ('STRENGTH', 'INTELLIGENCE', 'DEXTERITY', 'CONSTITUTION', 'HEALTH');</sql>
        <sql>alter type attribute_type owner to postgres;</sql>
        
        <!-- armor_class -->
        <sql>create type armor_class as enum ('LEATHER', 'HEAVY', 'CLOTH');</sql>
        <sql>alter type armor_class owner to postgres;</sql>
        
        <!-- spell_category -->
        <sql>create type spell_category as enum ('MELEE', 'RANGED', 'MAGIC');</sql>
        <sql>alter type spell_category owner to postgres;</sql>
        
        <!-- spell_impact_type -->
        <sql>create type spell_impact_type as enum ('DAMAGE', 'HEALING');</sql>
        <sql>alter type spell_impact_type owner to postgres;</sql>
        
        <!-- element -->
        <sql>create type element as enum ('FIRE', 'WATER', 'EARTH', 'AIR', 'LIGHTNING', 'ICE', 'POISON', 'THUNDER', 'PHYSICAL', 'HOLY');</sql>
        <sql>alter type element owner to postgres;</sql>
        
        <!-- effect_type -->
        <sql>create type effect_type as enum ('BUFF', 'DE_BUFF');</sql>
        <sql>alter type effect_type owner to postgres;</sql>
        
        <!-- item_type -->
        <sql>create type item_type as enum ('ARMOR', 'WEAPON', 'POTION', 'TROPHY');</sql>
        <sql>alter type item_type owner to postgres;</sql>
    </changeSet>

</databaseChangeLog>